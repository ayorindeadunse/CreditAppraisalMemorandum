@model Wema_CAM.Models.LoginModel

@{
    ViewBag.Title = "Log in";
}

<script src="@Url.Content("~/Scripts/jquery-1.9.0.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery-ui-1.9.2.min.js")" type="text/javascript"></script>

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>

@if (ViewBag.loginerror != null)
{

   <h3>@ViewBag.loginerror</h3>
}

<style>
     .tblboda {
    border-width: 1px;
    border-style: solid;
    border-color: #000000;
    padding: 5px;  
    
      /*padding-right: 0px;  
      padding-bottom: 0.25in;  
      padding-left: 5em;*/  
      max-width:1500px;
    }

    .tblboda1 {
        border-width: 3px;
        border-style: solid;
        border-color: #000000;
        padding: 5px;
        max-width: 1500px;
        /*padding-right: 0px;  
      padding-bottom: 0.25in;  
      padding-left: 5em;*/
    }
   
</style>

@*<section id="loginForm">*@

@using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        @if(ViewBag.NoEmailError != null) {
        <p>@ViewBag.NoEmailError</p>
        }
        <legend>Log in Form</legend>
        <ol>
            <li>
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName)
                @Html.ValidationMessageFor(m => m.UserName)
            </li>
            <li>
                @Html.LabelFor(m => m.Password)
                @Html.PasswordFor(m => m.Password)
                @Html.ValidationMessageFor(m => m.Password)
            </li>
           @* <li>
                @Html.CheckBoxFor(m => m.RememberMe)
                @Html.LabelFor(m => m.RememberMe, new { @class = "checkbox" })
            </li>*@
        </ol>
      <input type="submit" value="Log in"  /> 
       
  @*<div id="dialog-message" title="Message"></div>     
<div id="resetpass" class="tblboda1">
    <h3>Enter your E-mail in the field provided and click on the button to reset your password if you have forgotton it.</h3>
    <input type="text" id="resetppassword" name="resetppassword" />
    <br />
    <br />
    <input type="submit" id="setpass" value="Reset Password" />

   
</div>*@
    </fieldset>@*
   
    <p>
        @Html.ActionLink("Register", "ChangePassword","CAMViewer") 
    </p>*@
}
@*</section>*@

@*<section class="social" id="socialLoginForm">
    <h2>Use another service to log in.</h2>
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}



@*<script type ="text/javascript">
    //Approve Button click
    $(document).ready(function () {
        //$.testt();
        
        if( $("#setpass").click(function (e) {
               e.preventDefault();
               $.ajax({
            type: "POST",
                url: "/Account/ChangePassword",
            datatype:"json",
            data: {
            resetppassword: $("#resetppassword").val(),
        },
            success: function (data, textStatus, jqXHR) {

            $("#dialog-message").text(data.Message);
            //$("#dialog-message").html(data.Feedback);
            $("#resetppassword").val("");
                       $("#dialog-message").dialog({
            modal: true,
            buttons: {
            Ok: function () {
                                   $(this).dialog("close");
        }
        }
        });
        }
        });
        }));
    });
         
</script>*@